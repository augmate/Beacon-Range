<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              android:gravity="center"
              tools:context="${packageName}.${activityClass}">

    <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceSmall"
            android:text="Small Text"
            android:id="@+id/debug" android:layout_gravity="center_horizontal"
            android:layout_alignParentTop="true" android:layout_centerHorizontal="true"/>
    <ListView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="8px"
            android:id="@+id/list" android:layout_gravity="center_horizontal"
            android:layout_below="@+id/debug" android:layout_alignParentStart="true"
            android:layout_above="@+id/status" android:longClickable="false" android:textFilterEnabled="false"
            android:clickable="false"/>
    <TextView
            android:drawablePadding="10dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:drawableLeft="@android:drawable/ic_dialog_info"
            android:text="@string/status"
            android:textSize="26dp"
            android:id="@+id/status"
            android:layout_centerHorizontal="true" android:layout_alignParentBottom="true"
            />

</RelativeLayout>